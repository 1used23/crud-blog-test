// import { LOGIN_SUCCESSFUL } from "../constants";

const initialState = {
  comments: null
};

export const commentsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
